# Supabase migrations

Apply migrations in filename order. For manual setup, paste each file into the
Supabase SQL editor (see ../docs/SUPABASE_SETUP.md). If you later adopt the
Supabase CLI, this directory follows its conventional layout.
